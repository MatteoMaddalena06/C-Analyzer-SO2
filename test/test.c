#include <stdio.h>
#define MACRO_0 4 
#define MACRO_1 45

/*
    Commento multiriga
*/

//commento su una riga

typedef struct struct$con nome %&891 sbagliato {
    int var1; 
    int var2;
    int c23%!)campo cone___!!! nome sbagliato;
} tipo_sbagliato$$;

typedef unsigned long int uLongInt;
typedef uLongInt 2typedef$%sbagliato; 
typedef long int sLongInt;

uLongInt gloabl_var1 = var_esterna1 + var_esterna2;

void function(int parametro_corretto, tipo_sbagliato$$ parametro£$£%£_%sbagliato)
{
    return funzione_esterna(parametro_corretto + log(e*3));
}

int main(int argc, char* args)
{
    tipo_sbagliato$$ b = (uLongInt) 34.5;
    2typedef$%sbagliato local_var = 34 + function(10, b);
    sLongInt local_var2 = 34 / b;

    char* stringa = "io sono una stringa";
    char carattere = 'i';

    printf("%d %d", local_var, local_var2);

    return carattere;
}




